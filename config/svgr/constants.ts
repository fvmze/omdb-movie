export const DEFAULT_SVG_PROPS = {
  role: 'img',
  focusable: 'false',
  'aria-hidden': 'false',
} as const

export const DEFAULT_REPLACE_ATTR_VALUES = {
  '#000': 'currentColor',
  '#111827': 'currentColor',
} as const
